from setuptools import setup

install_requires = []

setup(
    name='pattern_translate',
    version='1.0.2',
    packages=['pattern_translate'],
    install_requires=install_requires,
    author='Esteban Parra Mesa'
)
